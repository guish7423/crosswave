# CrossDeploy — Deploy as a Railway service
[build]
dockerfile = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
healthcheckPath = "/health"
healthcheckTimeout = 10

[service]
port = 8002
